// @flow
import firebase from 'react-native-firebase';
// Optional flow type

export default async message => {
  // handle your message
  console.log('------', message);
  return Promise.resolve();
};
